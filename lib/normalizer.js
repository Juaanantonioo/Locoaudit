"use strict";

/**
 * normalizer.js — Convierte salidas de herramientas externas al esquema Finding.
 *
 * Exporta:
 *   fromNmap(raw)    → Finding[]
 *   fromTrivy(raw)   → Finding[]
 *   fromNuclei(raw)  → Finding[]
 */

// TODO: implementar

module.exports = {};
