"use strict";

/**
 * executor.js — Lanzador de procesos externos con timeout y manejo de errores.
 *
 * Exporta:
 *   run(cmd, args, options) → Promise<{ stdout, stderr, code }>
 */

// TODO: implementar

module.exports = {};
